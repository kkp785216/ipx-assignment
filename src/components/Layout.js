export const Section = ({children}) => {
    return (
        <section className="max-w-[1197px] mx-[clamp(20px,7vw,44px)] lg:ml-[6.45833333%] lg:mr-[10.277778%] sc1440:m-auto">
            {children}
        </section>
    )
}